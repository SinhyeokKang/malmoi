# Invitation email — tasks

상태: 미구현. [spec.md](./spec.md)·[design.md](./design.md)가 계약이다. 이번 feature 단계에서는 문서만 작성하고 코드·정본 문서·커밋은 변경하지 않는다.

## T0. 디자인 범위

- [x] **T0.1** Resend·본문 링크 한 줄·다중 초대·사람별 역할·성공 시 닫기를 확정했다. 같은 주소 60초, 프로젝트 최근 1시간 20건, 재발급 시 이전 토큰 만료도 확정했다.
  - 검증: 대화의 사용자 결정과 spec/design을 대조했다.
- [x] **T0.2** 불필요한 분기를 줄이는 사용자 위임과 **Pending Resend 유지**를 반영했다. 부분 결과 화면·주소별 발송 상태·링크 복사를 제거하고 전체 사전 검증으로 통일했다.
  - 검증: 데스크탑 디자인 피드백도 같은 방향으로 대체했다. 디자인 링크는 spec §1.1에 보존한다.
- [ ] **T0.3** 단순화한 디자인 수정본을 확인한다. `design_handoff_invite_modal 2/`의 README·전달 프롬프트 대조 완료: 단일 폼과 Resend 유지가 일치하고 카드 Alert 위치를 design §6에 반영했다. 발급 건수·재시도 시각·workspace 문구는 같은 절에서 보정한다. **수정본 렌더·실제 조작은 미검증**이다.
  - 검증: 폼 입력→전송→닫힘, 오류 시 같은 폼, Pending Resend를 검토하고 1280×720 긴 목록에서 footer/포커스를 확인한다.

## T1. 순수 함수 — 테스트 먼저

- [ ] **T1.1** 다중 입력·역할 보존·중복 거부·전체 발급 제한·메일 payload·설정·요청 단위 결과 판정 테스트를 먼저 작성한다.
  - 검증: 구현 전 red. 특히 한 명이 이미 멤버/간격 제한이면 전체 차단, 19건 상태의 3명 요청 거부, 시간창 정각을 단언한다.
- [ ] **T1.2** design §2의 순수 함수를 구현한다. 주소별 발송 상태/성공자 집계 함수는 만들지 않는다.
  - 검증: 순수 테스트 green. 같은 주소의 역할 충돌을 조용히 합치지 않고 메일 본문이 URL 한 줄이다.

커밋 경계 1: `feat: define invitation email policies`

## T2. 서버 발급·메일·Resend

- [ ] **T2.1** 서버 전용 Resend `/emails/batch` 호출과 env 배선을 구현한다. 10초 timeout·자동 재시도 없음·요청 UUID 멱등 키. `.env.example`도 갱신한다.
  - 검증: mock HTTP로 접수/명시적 거부/불완전·timeout·5xx를 검사하고 주소·토큰·API 키가 로그에 없는지 확인한다.
- [ ] **T2.2** `createInvitations`를 전체 사전 검증→Project 잠금 안 전체 회전·생성·사건→commit 후 발송으로 구현한다. 설정 불가도 생성 전에 막는다. 기존 단건 호출부와 하네스를 전환한다.
  - 검증: 한 대상이라도 사전 거부면 쓰기·발송0, 중간 DB 실패는 전체 롤백, 메일 실패는 저장 유지, 성공/실패 응답 모두 토큰·URL 없음. `membership.test.ts`와 Action 회귀 green.
- [ ] **T2.3** `resendInvitation({slug, invitationId})`를 추가한다. 서버의 기존 주소/역할로 재발급하고 같은 제한·발송 경로를 쓴다.
  - 검증: OWNER만 가능, 타 프로젝트/철회/수락/만료된 id/복호화 실패는 무변경. 새 만료7일·inviter·사건, 기존 토큰 무효, API 결과 마스킹을 단언한다.
- [ ] **T2.4** 격리 PostgreSQL에 동시 초대·Resend·Revoke·수락·한도 경합 검사를 추가한다.
  - 검증: 마지막 한도 자리의 동시 요청은 하나만 통과, 60초 우회 없음, 수락 CAS 보존. `pnpm test:projects:postgres` green. 환경이 없으면 미검증으로 남긴다.

커밋 경계 2: `feat: send and resend invitation emails`

## T3. UI

- [ ] **T3.1** 모달을 행별 이메일·역할·추가/삭제·붙여 넣기 폼으로 바꾼다. 성공은 닫기+토스트, 오류는 입력 유지+행 오류/폼 Alert. 기존 링크 화면과 복사 로직을 제거한다.
  - 검증: DOM에서 역할 보존·중복·붙여 넣기·Enter/IME·pending 잠금·통신 오류 해제·성공 닫기·오류 입력 보존·행 삭제 후 포커스/가짜 성공 없음. 단순화와 무관한 기존 코드는 고치지 않는다.
- [ ] **T3.2** Pending에 Resend를 붙인다. 처리 중 해당 행 Resend/Revoke 잠금, 결과는 목록 재검증 후에도 살아 있는 부모 피드백으로 전달한다.
  - 검증: 성공/거부/unknown, 교체된 행 id에서도 메시지 미소실, 마스킹 라벨, 새 모달·링크·배지 없음. 실제 브라우저에서 긴 목록·1280×720·키보드 확인.
- [ ] **T3.3** 기존 로그인·초대 수락 회귀 및 전체 게이트를 실행한다.
  - 검증: GET/HEAD 미소비, 다른 이메일·만료·옛 토큰 거부. `pnpm typecheck` + `pnpm test` green.

커밋 경계 3: `feat: streamline the invitation modal and add resend`

## T4. 문서·운영·실제 왕복

- [ ] **T4.1** PRODUCT §4.1/4.2/4.3, ARCHITECTURE §6.02, DESIGN, DIRECTORY를 최종 구현과 맞춘다. 직접 링크 전달은 새 UI에서 제거됐음을 명시한다.
  - 검증: 정본에 미래 구현을 완료형으로 쓰지 않고 메일 장애 시 초대 지연과 Resend 복구 경로를 설명한다.
- [ ] **T4.2** 실제 개인정보 방침과 OPERATIONS에 Resend 전송처·주소/링크·실제 보존 설정·키 교체·오류 확인 절차를 반영한다.
  - 검증: 실제 외부 전송과 문서 대조, 관련 기존 개인정보 테스트 green.
- [ ] **T4.3** 제목·인증 가능한 발신 주소를 운영에서 확정하고 환경별 키/from/origin·DNS(SPF/DKIM/DMARC)·추적 비활성화를 설정한다.
  - 검증: 키 원문을 기록하지 않고 설정 유효성을 확인한다. `.env.local`은 에이전트가 편집하지 않는다.
- [ ] **T4.4** 사용자 지정 수신자로 dev에서 역할 혼합 다중 초대→개별 수신→로그인→수락, Pending Resend→옛 링크 거부를 확인한다.
  - 검증: 메일은 자기 링크 한 줄, 다른 주소 없음, dev 착지, 서버 저장 역할 일치. 실제 메일은 지정 수신자가 있을 때만 보낸다.
- [ ] **T4.5** 프로덕션 배포 뒤 지정 수신자로 prod 왕복을 확인한다. 장애 시 초대 발송을 중단하고 기존 로그인·멤버 접근은 유지한다.
  - 검증: prod 초대가 prod에서 수락된다. 미실행이면 운영 활성화 완료로 기록하지 않는다.

커밋 경계 4: 문서별로 분리. Codex 커밋에는 지정 트레일러를 붙인다. 원격 push/merge는 Claude Code의 `/push`·`/merge`가 담당한다. 이 스펙 단계에서는 배포·계정 생성·DNS 변경·실제 메일 발송을 하지 않는다.
